# ─── RPA Recorder / Replayer ──────────────────────────────────────────────────
# Python 3.10+  |  Windows (PyAutoGUI + pynput + pywin32 natifs)

# Automatisation bureau
pyautogui>=0.9.54
pynput>=1.7.6

# OCR
easyocr>=1.7.1

# Traitement image
Pillow>=10.0.0
numpy>=1.24.0

# Détection multi-moniteurs (clippe les screenshots aux limites de chaque écran)
screeninfo>=0.8.1

# Dashboard web (v3+)
flask>=3.0.0

# Interface graphique moderne (v4)
customtkinter>=5.2.0

# Capture du nom d'application Windows en premier plan (v4)
pywin32>=306
psutil>=5.9.0

# Icône de zone de notification pour le mode automatique (v5)
pystray>=0.19.4

# (optionnel) accélération GPU pour EasyOCR
# torch>=2.0.0        # décommenter si GPU CUDA disponible
# torchvision>=0.15.0
