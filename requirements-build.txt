# ─── Dépendances du BINAIRE Windows (PyInstaller) ────────────────────────────
# EasyOCR/torch supprimés en v6.6 → remplacés par opencv-python-headless.
# La localisation dynamique (template matching OpenCV) est incluse dans le build.

# Automatisation bureau
pyautogui>=0.9.54
pynput>=1.7.6

# Traitement image + template matching (remplace EasyOCR/torch)
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0

# Détection multi-moniteurs
screeninfo>=0.8.1

# Dashboard web
flask>=3.0.0

# Interface graphique moderne
customtkinter>=5.2.0

# Capture du nom d'application Windows en premier plan
pywin32>=306
psutil>=5.9.0

# Icône de zone de notification (mode automatique)
pystray>=0.19.4

# Backend clavier de secours (hook bas niveau WH_KEYBOARD_LL)
keyboard>=0.13.5

# Outil d'empaquetage
pyinstaller>=6.3.0
